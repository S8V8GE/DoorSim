<UserControl x:Class="DoorSim.Views.SingleDoorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E"
             Foreground="White">

    <!--
    Single door display view.

    Responsible for:
    - Showing the currently selected door
    - Later displaying door hardware visually
      (readers, REX, break glass, lock, sensor, etc.)

    This is currently a placeholder.
    -->

    <Grid>
        <Border BorderBrush="#3F3F46"
                BorderThickness="1"
                CornerRadius="6"
                Padding="20"
                Margin="20"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">

            <StackPanel HorizontalAlignment="Center">

                <TextBlock Text="{Binding SelectedDoor.Name}"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#CCCCCC"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Single door visual will appear here"
                           FontSize="16"
                           Foreground="#888888"
                           Margin="0,12,0,0"
                           HorizontalAlignment="Center"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>